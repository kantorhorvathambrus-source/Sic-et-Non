{
  "name": "sic-et-non",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "npm run verify:content && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "linkcheck": "node scripts/link-check.mjs",
    "verify:content": "node scripts/verify-quotes.mjs",
    "milestone": "npm run check && npm run build && npm run linkcheck && npm run check:layout && npm run check:palette && npm run check:style",
    "search:index": "pagefind --site dist",
    "check:layout": "node scripts/layout-check.mjs",
    "check:palette": "node scripts/palette-check.mjs",
    "check:style": "node scripts/style-check.mjs",
    "verification:sheet": "node scripts/verification-sheet.mjs"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource/eb-garamond": "^5.3.0",
    "astro": "^5.14.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "pagefind": "^1.4.0",
    "playwright": "^1.62.1",
    "typescript": "^5.9.2"
  }
}
